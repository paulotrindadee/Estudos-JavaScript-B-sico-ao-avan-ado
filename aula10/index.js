// Tipos de dados primitivos são;  String, number, underfined, null e boolean.

const nome = 'luiz' //string
const nome = 'luiz' //string
const nome = `luiz` //string
const num1 = 10 // number
const num2 = 10.52 // number
let nomeAluno // underfined =  não aponta para local nenhum na memória
const sobrenomeAluno = null // Não aponta para nenhum local na memória
const boolean = true // Boolean =  false, true (Lógico) (lógica de um código) Faz um desvio condicional
